{
  "fixtureName": "bm_build_missing",
  "description": "One feature omits the genome build in its measuredRegion. The GenomicRegion schema requires 'build' to be present, so schema validation should fail for that feature.",
  "expectedSchemaValid": false,
  "expectedQualificationStatus": "packet_schema_invalid",
  "expectedWarnings": ["EPI001_PACKET_SCHEMA_INVALID"],
  "expectedBlocksDownstream": true,
  "expectedHandoffReady": false
}
